<h1>タスク一覧</h1>

<ul>
  <% @messages.each do |message| %>
  <li><%= link_to message.id, message %> : <%= message.status %> &gt; <%= message.content %></li>
  <% end %>
</ul>
<%== pagy_nav(@pagy) %>

<%= link_to '新規タスクの追加', new_message_path %>
